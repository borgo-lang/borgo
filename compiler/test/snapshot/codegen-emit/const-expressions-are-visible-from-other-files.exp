Const expressions are visible from other files

SOURCE:
file: foo.brg

fn check() -> bool {
  foo == 2
}

file: main.brg

const foo: int = 1 + 1
fn main() { assert_eq(check(), true) }

OUTPUT:

---
package main

var foo = func() int {
	return 1 + 1
}()

func main() {
	assert_eq[bool](check(), true)
}
