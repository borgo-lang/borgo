Primitive types are casted in struct call

SOURCE:
struct Foo {
    bar: int,
}

fn main() {
    let x = 1
    let y = Foo { bar: x }
    assert_eq(y.bar, 1)
}

OUTPUT:

---
package main

type Foo struct {
	bar int
}

func main() {
	x := 1

	y := Foo{bar: x}

	assert_eq[int](y.bar, 1)
}
