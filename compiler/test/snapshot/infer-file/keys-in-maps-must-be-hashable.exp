Keys in Maps must be hashable

SOURCE:
fn foo<K: to_hash>(k: K) {}

fn borgo_main() {
  foo(|| {});
  unreachable!();
}

OUTPUT:
Functions don't support overload `to_hash`

4|    foo(|| {});
      ^^^^^^^^^^