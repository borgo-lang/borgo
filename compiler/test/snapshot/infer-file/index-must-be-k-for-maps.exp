Index must be K for maps

SOURCE:
fn borgo_main() {
    let xs = Map::new();
    xs.insert("a", 1);
    xs[false]
}

OUTPUT:
Type mismatch
Expected:
    string
Got:
    bool

4|      xs[false]
        ^^^^^^^^^