Deriving will error on functions

SOURCE:
fn bar() {}

fn foo() -> Bool {
  equals(bar, bar) as Bool;
}

OUTPUT:
Functions don't support overload `equals`

4|    equals(bar, bar) as Bool;
      ^^^^^^^^^^^^^^^^