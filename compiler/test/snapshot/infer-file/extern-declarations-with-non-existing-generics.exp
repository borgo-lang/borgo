Extern declarations with non existing generics

SOURCE:
struct Foo {}

#[package(path = github.com:yo/test, name = test::pkg)]
mod f {
  extern {
    fn bar<T>(x: T, k: K);
  }
}

fn borgo_main() {
  1.assert_eq(2)
}

OUTPUT:
Type not found: K

6|      fn bar<T>(x: T, k: K);
        ^^^^^^^^^^^^^^^^^^^^^^