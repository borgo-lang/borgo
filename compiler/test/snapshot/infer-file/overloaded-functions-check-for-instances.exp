Overloaded functions check for instances

SOURCE:
struct Foo {
  x: Int,
  y: fn() -> Int,
}

fn borgo_main() -> Bool {
  let f = Foo { x: 1, y: || 2 };
  let _ = Foo::to_string(f); // this is ok
  equals(f, f)
}

OUTPUT:
No overload `equals` found for type `Foo`

9|    equals(f, f)
      ^^^^^^^^^^^^